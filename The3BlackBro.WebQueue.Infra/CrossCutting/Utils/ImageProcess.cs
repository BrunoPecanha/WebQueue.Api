using System.IO;

namespace The3BlackBro.WebQueue.Infra.CrossCutting.Utils {
    public class ImageProcess {

        //    public byte[] ImageToByteArray(byte[] imageIn) {
        //        BinaryReader[] byteimage = null;

        //        using (var binaryReader = new BinaryReader(imageIn))
        //            byteimage = binaryReader.ReadBytes(imageIn.Length);
        //    }

        //    public Image ByteArrayToImage(byte[] byteArrayIn) {
        //        using (var ms = new MemoryStream(byteArrayIn)) {
        //            var returnImage = Image.FromStream(ms);

        //            return returnImage;
        //        }
        //    }
    }
}

